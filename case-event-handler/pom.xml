<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>com.bonitasoft.handler</groupId>
	<artifactId>case-event-handler</artifactId>
	<version>1.0.0-SNAPSHOT</version>

	<name>Case Event Handler</name>
	<description>A Bonita event handler for case management</description>

	<properties>
		<bonita.version>7.8.1</bonita.version>
		<groovy.version>2.4.16-02</groovy.version>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<!-- WARNING for Windows users: keep forward slashes like below (instead of backslashes) -->
		<bonita.project.name>bonita-case-management</bonita.project.name>
		<studio.workspace.path>/home/romain/Téléchargements/BonitaStudioSubscription-7.9.0-ACM/workspace</studio.workspace.path>
		<platform.setup.dir>${studio.workspace.path}/tomcat/setup</platform.setup.dir>
		<target.db.path>${studio.workspace.path}/${bonita.project.name}/h2_database</target.db.path>
	</properties>

	<dependencies>
		<dependency>
			<groupId>org.bonitasoft.engine</groupId>
			<artifactId>bonita-server</artifactId>
			<version>${bonita.version}</version>
			<scope>provided</scope>
		</dependency>
		<dependency>
			<groupId>org.codehaus.groovy</groupId>
			<artifactId>groovy-all</artifactId>
			<version>2.4.16</version>
			<scope>provided</scope>
		</dependency>
		<dependency>
			<groupId>org.slf4j</groupId>
			<artifactId>slf4j-api</artifactId>
			<version>1.6.1</version>
			<scope>provided</scope>
		</dependency>
	</dependencies>

	<build>
		<finalName>${project.artifactId}</finalName>
		<resources>
			<resource>
				<directory>src/main/resources</directory>
			</resource>
		</resources>

		<sourceDirectory>src/main/groovy</sourceDirectory>
		<testSourceDirectory>src/test/groovy</testSourceDirectory>

		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.6.0</version>
				<configuration>
					<source>1.8</source>
					<target>1.8</target>
					<compilerId>groovy-eclipse-compiler</compilerId>
				</configuration>
				<dependencies>
					<dependency>
						<groupId>org.codehaus.groovy</groupId>
						<artifactId>groovy-eclipse-compiler</artifactId>
						<version>3.0.0-01</version>
					</dependency>
					<dependency>
						<groupId>org.codehaus.groovy</groupId>
						<artifactId>groovy-eclipse-batch</artifactId>
						<version>${groovy.version}</version>
					</dependency>
				</dependencies>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-dependency-plugin</artifactId>
				<version>2.10</version>
				<executions>
					<execution>
						<id>copy-handler-to-studio-tomcat</id>
						<phase>package</phase>
						<goals>
							<goal>copy</goal>
						</goals>
						<configuration>
							<artifactItems>
								<artifactItem>
									<groupId>${project.groupId}</groupId>
									<artifactId>${project.artifactId}</artifactId>
									<version>${project.version}</version>
									<type>${project.packaging}</type>
								</artifactItem>
							</artifactItems>
							<overWriteIfNewer>true</overWriteIfNewer>
							<overWriteReleases>true</overWriteReleases>
							<overWriteSnapshots>true</overWriteSnapshots>
							<outputDirectory>${studio.workspace.path}/tomcat/server/webapps/bonita/WEB-INF/lib</outputDirectory>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-resources-plugin</artifactId>
				<version>3.0.2</version>
				<executions>
					<execution>
						<id>copy-spring-conf-to-studio-tomcat</id>
						<phase>package</phase>
						<goals>
							<goal>copy-resources</goal>
						</goals>
						<configuration>
							<resources>
								<resource>
									<directory>src/main/resources</directory>
									<include>bonita-tenant-sp-custom.xml</include>
								</resource>
							</resources>
							<overwrite>true</overwrite>
							<outputDirectory>${platform.setup.dir}/platform_conf/current/tenants/1/tenant_engine</outputDirectory>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

	<profiles>
		<profile>
			<id>win</id>
			<activation>
				<os>
					<family>Windows</family>
				</os>
			</activation>
			<build>
				<plugins>
					<plugin>
						<artifactId>exec-maven-plugin</artifactId>
						<groupId>org.codehaus.mojo</groupId>
						<version>1.6.0</version>
						<executions>
							<execution>
								<id>Pull engine conf (Windows)</id>
								<phase>prepare-package</phase>
								<goals>
									<goal>exec</goal>
								</goals>
								<configuration>
									<workingDirectory>${platform.setup.dir}</workingDirectory>
									<executable>setup.bat</executable>
									<arguments>
										<argument>pull</argument>
										<argument>-Dh2.database.dir=${target.db.path}</argument>
									</arguments>
								</configuration>
							</execution>
							<execution>
								<id>Push engine conf (Windows)</id>
								<phase>install</phase>
								<goals>
									<goal>exec</goal>
								</goals>
								<configuration>
									<workingDirectory>${platform.setup.dir}</workingDirectory>
									<executable>setup.bat</executable>
									<arguments>
										<argument>push</argument>
										<argument>-Dh2.database.dir=${target.db.path}</argument>
									</arguments>
								</configuration>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
		<profile>
			<id>linux</id>
			<activation>
				<os>
					<family>Unix</family>
				</os>
			</activation>
			<build>
				<plugins>
					<plugin>
						<artifactId>exec-maven-plugin</artifactId>
						<groupId>org.codehaus.mojo</groupId>
						<version>1.6.0</version>
						<executions>
							<execution>
								<id>Pull engine conf (linux)</id>
								<phase>prepare-package</phase>
								<goals>
									<goal>exec</goal>
								</goals>
								<configuration>
									<workingDirectory>${platform.setup.dir}</workingDirectory>
									<executable>${platform.setup.dir}/setup.sh</executable>
									<arguments>
										<argument>pull</argument>
										<argument>-Dh2.database.dir=${target.db.path}</argument>
									</arguments>
								</configuration>
							</execution>
							<execution>
								<id>Push engine conf (linux)</id>
								<phase>install</phase>
								<goals>
									<goal>exec</goal>
								</goals>
								<configuration>
									<workingDirectory>${platform.setup.dir}</workingDirectory>
									<executable>${platform.setup.dir}/setup.sh</executable>
									<arguments>
										<argument>push</argument>
										<argument>-Dh2.database.dir=${target.db.path}</argument>
									</arguments>
								</configuration>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>
</project>
